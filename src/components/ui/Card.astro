---
import consultationImage from "@assets/images/consultation.png";
import interviewImage from "@assets/images/interview.png";
import growthPlanImage from "@assets/images/growth-plan.png";

import { Image } from "astro:assets";
import Tags from "@components/ui/Tags.astro";

interface Props {
  url?: string;
  alt?: string;
  title?: string;
  description: string;
  tags: string[];
}

interface ImageMap {
  [key: string]: ImageMetadata;
}

const imageMap: ImageMap = {
  "Konsultacje online 1:1": consultationImage,
  "Rozmowa rekrutacyjna": interviewImage,
  "Ścieżki kariery": growthPlanImage,
};

const {
  url = "#",
  alt = "Project",
  description = "Project Description",
  title = "Project title",
  tags = ["Tags"],
} = Astro.props;

const image = imageMap[title];
---

<section class="w-full p-4 lg:w-1/3">
  <article
    data-testid="card"
    class="rounded-xl bg-white dark:bg-gray-800 p-3 shadow-lg dark:shadow-orange/20 duration-100 hover:scale-105 hover:transform hover:shadow-xl"
  >
    <a href={url} aria-label={title ? `Zobacz ofertę: ${title}` : "Zobacz szczegóły oferty"}>
      <div
        class="relative flex items-end justify-center overflow-hidden rounded-xl"
      >
        <Image
          src={image}
          alt={alt}
          layout="constrained"
          width={400}
          priority
        />
      </div>
      <div class="mt-1 p-2">
        <h3 class="text-xl text-orange lg:text-2xl">{title}</h3>
        <p class="text-black dark:text-white text-s">{description}</p>
        <Tags tags={tags} />
      </div>
    </a>
  </article>
</section>
